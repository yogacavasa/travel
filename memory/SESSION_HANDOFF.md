# 🤝 SESSION HANDOFF — Kondisi Terakhir untuk Agent Berikutnya
## Travel & Fleet Management Ecosystem

> Dibaca otomatis oleh `bash scripts/load_context.sh`. Isi SINGKAT & AKURAT di akhir tiap sesi.

---


## STATUS TERKINI
- **RESTORE dari GitHub `shsjskhd/Travel` ke pod baru + FASE CMS-CW2 (CMS-05…CMS-09 + defect A1/A2/A3) SELESAI & TERVERIFIKASI (2026-08-17).**
  Repo di-clone lalu di-rsync PENUH ke `/app` (`.env` platform DIPERTAHANKAN, hanya DITAMBAH `SETTINGS_ENCRYPTION_KEY_B64` + `PUBLIC_SITE_URL` + `MEDIA_BACKEND=local`); `bash scripts/bootstrap.sh` 64 detik → backend & frontend RUNNING.
  **Verifikasi dilakukan LEBIH DULU sebelum menyentuh kode:** `gate.sh` = **41 PASS / 1 FAIL** (FAIL tunggal `ux_audit --strict`: `PackageDetail.jsx` tanpa empty-state) dan POC `scripts/test_core_cms_cw2.py` = **82/86**.
  **TITIK BERHENTI SESI LALU = WIRING FRONTEND.** Backend CMS-05…CMS-09 sudah ada (`content_publish`, `i18n`, `richtext`, `reviews`, `content_stats`, endpoint publik & sitemap hreflang), tetapi `Packages/PackageDetail/Promos/ReviewSubmit/LanguageSwitch` **belum di-route** dan UI admin belum punya jadwal terbit, tab bahasa, editor rich text, moderasi ulasan, analitik konten, maupun field aturan promo.

  **KEPUTUSAN USER (dikonfirmasi):** (1) kerjakan semua sisa plan sampai tuntas; (2) **terjemahan English MANUAL — AI OFF** (endpoint translate tetap ada, membalas **503 berpesan "isi manual"**, + rate-limit 12/5 menit → 429); (3) URL publik **bahasa Inggris** (`/packages`, `/packages/:slug`, `/promo`); (4) WhatsApp/Meta/Google/GA4 tetap **MOCK**.

  **YANG DIKERJAKAN:**
  · **Publik:** route `/packages`, `/packages/:slug`, `/promo`, `/review/:token`; menu **Paket** (menu utama jadi 6) + utilitas **Promo** & **pemilih bahasa ID/EN** di bar pengumuman + drawer; `syncLangFromUrl()` tiap navigasi; `<html lang>`; `useSEO` menambah **hreflang id/en/x-default + og:locale** (`i18n:false` untuk `/lp/:slug`); `ArticleBody` merender HTML artikel tersanitasi (fallback teks polos lama); `PackageStrip`/`PromoStrip` menaut `/packages/:slug` & `/promo`.
  · **CMS admin:** filter **status terbit** + filter **bahasa (sudah/belum ada EN)**, badge status memakai `effective_status` + badge **EN**, tombol **tautan pratinjau bertoken** (24 jam) untuk konten belum tayang, `publicPath` diperbaiki ke route yang benar-benar ada (**A1**); dialog konten 3 tab **Indonesia · English · Terbit**; **RichTextEditor** (toolbar + mode HTML + konversi teks lama) untuk `articles.body`; field **aturan promo** lengkap + `used_count` read-only (**A2**); tab baru **Ulasan** (moderasi + kirim tautan, WA MOCK) dan **Analitik** (KPI + peringkat konten + ekspor CSV); endpoint baru `GET /api/content/meta/promo-options` (SSOT tipe armada + layanan).
  · **Fase 3:** rate-limit endpoint translate, filter locale, chip **atribusi konten** pada lead CRM (`lead-content-ref`), dan **dua bahasa untuk chrome halaman storefront** (Home, Destinations, DestinationDetail, Fleet, Blog, BlogDetail, PackageStrip, PromoStrip) lewat helper baru `bi(id, en, lang)` di `lib/i18n.js`.

  **4 BUG NYATA ditemukan & ditutup sesi ini** (rincian `memory/BUG_REGISTRY.md`): **BUG-0128** mesin bersih-bersih data uji melewatkan dokumen **tanpa field `id`** (`content_stats`) → artefak POC tampil di panel Analitik ("gate hijau, produk tidak bersih" — kelas BUG-0127); **BUG-0129** dependency **`bleach` tidak pernah dipin** → sanitasi rich text *fail-closed* membuang SEMUA tag (3 cek POC merah, tanpa satu pun error di log); **BUG-0130** isi `<script>`/`<style>` tersimpan sebagai kalimat yang terbaca pengunjung; **BUG-0131** `BlogDetail` merender HTML artikel sebagai TEKS MENTAH. Ditambah 3 regresi buatan sendiri yang ditemukan & diperbaiki sebelum/oleh testing agent: `lang` di luar scope `FeaturedStory` (Blog blank), `key={p.id}` pada promo publik (id sengaja tidak dibocorkan), dan translate membalas 400 alih-alih 503 saat AI OFF.

  **VERIFIKASI AKHIR:** POC `scripts/test_core_cms_cw2.py` **87/87** · `bash scripts/gate.sh` **HIJAU 42/42 (0 FAIL, 0 SKIP)** · `ux_audit --strict` **0 ERROR 0 WARN** · `verify_delivery` **P0 36/36** · testing agent **iteration_92** (Fase 2: 9 user story LOLOS, 94.5%) & **iteration_93** (Fase 3: 94.7%, semua temuan sudah ditutup). Header publik terukur **91,25 px (1 baris)** — kontrak §0.1 aman.

  **SISA PEKERJAAN JUJUR:** (a) chrome halaman `/trip-calculator`, `/quotation`, `/about`, `/contact`, `/booking`, `/booking/status` **masih Bahasa Indonesia** saat mode EN (isi konten DB + navigasi sudah EN) — ini backlog EN berikutnya dan sudah dicatat; (b) isi EN konten harus diisi manual di CMS (tab English) — belum ada satu pun terjemahan pada data demo; (c) WA/Meta/Google/GA4 tetap MOCK; (d) uji beban konkurensi tinggi belum dijalankan.
  ⚠️ **Anti-bug-palsu sesi berikut:** (1) HMR sering menyajikan bundle LAMA sesudah menyentuh banyak berkas FE → `sudo supervisorctl restart frontend` sebelum menyimpulkan "fix tidak jalan" (kejadian 3× sesi ini, salah satunya tampak seperti halaman blank); (2) status terbit HANYA di tab **Terbit** — toggle `published`/`active` sengaja DIHAPUS dari form; (3) field EN kosong = fallback Indonesia (dirancang begitu); (4) `content_stats` tidak punya field `id` — hapus lewat `_id` (BUG-0128); (5) `gate.sh`/`seed_data.py` me-RESEED DB, jangan dijalankan di tengah pengujian UI.

## STATUS SEBELUMNYA
- **RESTORE dari GitHub `bsjanhsgxhx/travel` ke pod baru + BUG-0127 "DATA UJI BOCOR KE ERP" DITUTUP + VERIFIKASI ALUR END-TO-END TUNTAS (2026-08-13).** Repo di-clone lalu di-rsync PENUH ke `/app` (`.env` platform DIPERTAHANKAN, hanya DITAMBAH `SETTINGS_ENCRYPTION_KEY_B64` + `PUBLIC_SITE_URL` + `MEDIA_BACKEND=local`); `bash scripts/bootstrap.sh` (deps FE+BE 58 detik) → backend & frontend RUNNING. **Verifikasi dilakukan LEBIH DULU sebelum menyentuh kode:** `bash scripts/gate.sh` **HIJAU 40/40 (0 FAIL, 0 SKIP)**.
  **PERMINTAAN USER (2 poin):** (1) *"saya ingin cek flow dari awal hingga akhir apakah tidak ada masalah dan aman"*; (2) *"untuk data seed customer dihapus saja"* → diperjelas user: *"sebenarnya ada bug di skrip ada nama customer aaaaaaaaaaaaaaaaaaaa itu yang mengganggu, maksud saya hapus adalah seed ini, mending seed diperbaiki namun tetap ada data demo"*. Jadi **DATA DEMO TETAP ADA**, yang dibuang adalah **DATA SAMPAH skrip uji**.
  **1 BUG NYATA ditemukan & ditutup — BUG-0127, kelas "gagal SENYAP dengan gate HIJAU":** penjaga & smoke SENGAJA menulis lewat API sungguhan tetapi hanya menghapus dokumen UTAMA; SIDE-EFFECT-nya tidak. Akibat **157 dokumen sampah per satu kali `gate.sh`**: `customers` bernama **60.000 karakter "AAAA…"** (dari `selftest_booking_guards` mutasi **S01** yang sengaja melepas `max_length` → nilai tersimpan → kode di-revert TAPI dokumen tak pernah dihapus), `audit_logs` ber-`summary` **60.016 karakter**, Inbox berisi "Penjaga INV-PRICE-01"/"Penjaga INV-BOOK-02"/"Smoke Customer"/"Guard Lead …" (2 menunjuk customer terhapus = yatim), 34 notifikasi hantu, **35 dari 48** `events` + `automation_runs` artefak, CRM menyimpan lead & penawaran berisi karakter **NUL** + segmen "AdvSeg", Media Library menyimpan `guard-media-*`, dan `conversion_events` (outbox konversi iklan) **menumpuk permanen** karena tak pernah di-reset seed → angka konversi dasbor pemasaran membengkak. Pelajaran yang dikunci: **"gate hijau" ≠ "produk bersih"**.
  **FIX:** `scripts/guardrails/_common.py` mendapat **mesin bersih-bersih bersama** `purge_guard_artifacts()` + SSOT penanda (`GUARD_MARKERS`/`GUARD_PHONE_PREFIXES`/`PURGE_COLLECTIONS` 40 koleksi/`OVERLONG_RULES`) dengan cascade PENUH (payments·invoices·expenses·trips·locations·trip_shares·**payment_proofs + aset media bukti**·messages·conversations termasuk yang lahir dari lead uji & percakapan yatim·notification_tasks·events·automation_runs·audit_logs termasuk yang MENYEBUT kode `BK-00xx`·**conversion_events**·landing_stats) dan **dokumen bersumber `seed` DIKECUALIKAN** (data demo mustahil terhapus). Sebelas skrip penulis memanggilnya di `finally` (7 penjaga + `selftest_booking_guards` + `mutation_smoke` + 2 POC). `backend/services/audit.py` mendapat `_clip()` (summary 300 / snapshot 2000 / aktor 120). `seed_data.py` me-reset `conversion_events` + memanggil purge tiap reseed. Nama dokumen uji yang ambigu diberi penanda ("AdvSeg"→"Penjaga INV-5XX Segmen", "RaceProbe"→"Penjaga INV-IDENT").
  **GUARDRAIL BARU `INV-CLEAN-01`** (`scripts/guardrails/verify_no_test_pollution.py`, statik 18 cek + runtime pemindai 40 koleksi, **di-wire PALING AKHIR di gate** supaya kebocoran penjaga mana pun langsung memerahkan gate) + `selftest_no_test_pollution.py` (**5 mutasi MERAH↔HIJAU**: customer 60k karakter · percakapan "Penjaga INV-BOOK-02" · audit 60.016 karakter · panggilan purge dilepas · `_clip` dilepas). Penjaga ini SENGAJA **tidak** membersihkan apa pun — membersihkan di situ akan menutupi kebocoran. Alat baru: `scripts/purge_test_pollution.py` (`--dry-run`) & `scripts/db_snapshot.py` (`--save`/`--diff`, bukti "data uji tidak menumpuk").
  **VERIFIKASI AKHIR:** `gate.sh` **HIJAU 42/42 (0 FAIL, 0 SKIP)** **DAN database 0 artefak SESUDAH gate** (sebelumnya 157) · POC `test_core_booking_v1.py` **74/74** & `test_core_lp_f8.py` **84/84**, diff snapshot DB hanya `sessions` + audit `login` (aktivitas sah) · `testing_agent_v3` **iteration_89** (kebersihan data 100% + RBAC 20/20 — driver & marketing diblokir di semua halaman manager-only), **iteration_90** (ALUR END-TO-END: situs publik 9 halaman 100%, pemesanan online BK-0068 hold → bukti DP → ops verifikasi → **confirmed** 100%, keuangan & laporan 100%, 17 modul ERP 100%), **iteration_91** (ALUR E dispatch→perjalanan→selesai: assign → konfirmasi keberangkatan → enroute → arrived → driver check-in/out odometer 84.000→84.260 = **260 km** → trip completed; backend 13/13). Data demo dipulihkan bersih pasca-uji (4 customer, BK-0001..BK-0010, 4 armada, 2 sopir, 4 akun).
  **WA/Meta/Google/GA4 tetap MOCK.** **BERIKUTNYA (antrean `HANDOFF.md` §4):** kredensial nyata (P1), load test konkurensi (P1), halaman promo publik `/promo` (P2), notifikasi proaktif hold hangus (P2), GA4 Measurement Protocol server-side (P2).
  ⚠️ **Anti-bug-palsu untuk sesi berikut:** (a) kalau Anda membuat data uji lewat API, WAJIB pakai penanda di `GUARD_MARKERS` (mis. awali nama dengan "Penjaga INV-") atau nomor `0800000xxx`, lalu panggil `purge_guard_artifacts()` di `finally` — kalau tidak, gate MERAH di `guard:no_test_pollution`; (b) MEMBATALKAN booking BUKAN membersihkan (booking sengaja tak punya endpoint DELETE) — hapus lewat mesin purge; (c) `bash scripts/gate.sh` & `python scripts/seed_data.py` me-RESEED database → jangan dijalankan di tengah pengujian UI (booking uji akan hilang dan terlihat seperti bug); (d) drift `sessions`/audit `login` sesudah gate itu WAJAR (guard memang login), gunakan `scripts/db_snapshot.py --diff` untuk memisahkan drift sah dari polusi.

## STATUS SEBELUMNYA
- **RESTORE ke pod baru + FASE 7 "UI READABILITY & KEDALAMAN HALAMAN PUBLIK" SELESAI & TERVERIFIKASI (2026-08-12, sesi 3).** Repo `yaiankao/travel` di-restore ke `/app` (`bash scripts/bootstrap.sh --seed`; `.env` environment DIPERTAHANKAN, hanya DITAMBAH `SETTINGS_ENCRYPTION_KEY_B64` + `PUBLIC_SITE_URL` + `MEDIA_BACKEND=local`). **Verifikasi dilakukan LEBIH DULU sebelum menyentuh kode:** `bash scripts/gate.sh` **HIJAU 38/38 (0 FAIL, 0 SKIP)** dan `test_reports/iteration_85.json` sesi lalu terbukti SUDAH SELESAI dengan **0 bug** (titik berhenti sesi lalu resmi tertutup). **PERMINTAAN USER (7 poin, dari 4 screenshot) — semuanya dikerjakan:** (1) "UI kurang readable — kalau font putih maka elemen visual background-nya harus disesuaikan"; (2) halaman **Armada** terlalu sepi; (3) halaman **Destinasi** juga (usul user: FAQ, fun fact, narasi + CTA); (4) halaman **Kalkulator** juga; (5) CTA `/blog/:slug` **rusak**; (6) **posisi chat** terlalu di atas; (7) rename brand jadi **RahazaTrans**. **6 BUG NYATA ditemukan & ditutup (BUG-0121…0126), semuanya kelas "gagal SENYAP" (tidak ada error konsol):** BUG-0121 `to-[color:var(--primary)]` — token proyek ini TRIPLET HSL, jadi class itu invalid dan browser membuang SELURUH `background-image` → panel CTA blog transparan + teks putih di kertas putih; BUG-0122 `.glass-modal` memaku `hsla(0 0% 100%/.94) !important` untuk SEMUA mode + `.glass-3d::before` `mix-blend-mode: screen` opacity .55 tanpa mask → modal putih-di-atas-putih di dark & label kartu estimator tersapu di atas foto terang; BUG-0123 tombol close dialog `data-[state=open]:bg-accent` → bulatan warna; **BUG-0124 (ditemukan TESTING AGENT iter-86)** konten Radix yang di-PORTAL ke `<body>` berada di luar `[data-surface][data-theme]` → dialog & SEMUA dropdown memakai palet ERP terang saat situs gelap; **BUG-0125 (regresi buatan sendiri, ditemukan sendiri sebelum diserahkan)** `--surface-on-hero: var(--card)/.93` dideklarasikan di `:root` → `var()` disubstitusi DI :root (tema ERP terang) sehingga kartu tetap TERANG di mode gelap; BUG-0126 ChatWidget `bottom-40` + tinggi TETAP 440px → panel menembus header di viewport pendek. **FIX INTI:** `index.css` (kaca ikut mode; refraksi via `--refraction-opacity/-blend/-mask`; kelas baru `.glass-on-hero` + `.hero-scrim`; token `--glass-edge*` diturunkan di `.dark`; SSOT offset `--header-h/--sticky-cta-h/--fab-bottom/--panel-bottom` + z-index) · `ThemeContext` memasang `data-surface`/`data-theme` di **`<html>`** + membersihkannya saat unmount · `public-themes.css` diberi selector KEMBAR `[…].dark` untuk 4 preset (`.dark X` hanya cocok DESCENDANT). **HALAMAN PUBLIK DIPERKAYA (semua dari DATA NYATA, 0 mock):** komponen baru `CtaBand`, `VehicleTypeCompare`, `AirportRouteStrip`, `PromoStrip`, `PackageStrip`, `DestinationFacts`, `FaqBlock`; `/fleet` dapat quick-action bar (unit tayang / tipe bertarif / DP%), filter tipe, perbandingan tipe + tarif, standar unit, estimator, rute bandara flat, promo, FAQ, CTA; `/destinations` dapat fun fact dari `highlights`+`best_time`+`itinerary`, paket wisata (`price_from`), narasi 3 kartu, FAQ **dirangkum dari `destinations[].faqs`**, CTA; `/trip-calculator` dapat "cara kerja" 3 kartu, empty-state berisi TARIF NYATA yang bisa diklik, perkiraan DP, perbandingan tipe, rute flat, promo, FAQ harga, CTA. `BookingWizard` menerima query **`route`** (deep-link kartu rute bandara). **RENAME BRAND** di 30 berkas FE/BE + `scripts/seed_data.py` + dokumen DB (settings `company_info`, `booking_flow.payment.bank_accounts[].holder` → "PT RahazaTrans", articles, landing_pages) + email `halo@rahazatrans.id`. **GUARDRAIL BARU `INV-THEME-01`** (`scripts/guardrails/verify_theme_contrast.py`, 290 cek statik + `selftest_theme_contrast.py` **7 mutasi MERAH↔HIJAU**) menjaga: class gradient/warna arbitrary dengan token triplet, kaca dipaku putih (termasuk fallback `@supports`), `mix-blend-mode: screen` hardcode & `--refraction-opacity` > .30, komposisi token TEMA di `:root`/`.dark`, token offset mengapung + `ChatWidget` wajib memakainya & punya `maxHeight`, `ThemeContext` wajib set+clear atribut di `<html>`, dan kembaran selector `.dark` per preset. **VERIFIKASI AKHIR:** `gate.sh` **HIJAU 40/40 (0 FAIL, 0 SKIP)** · `ux_audit --strict` **0 ERROR 0 WARN** · testing agent `iteration_86.json` (7/8 item lolos, menemukan BUG-0124), `iteration_87.json` (**100 %** — fix terverifikasi + isolasi ERP aman termasuk uji BOLAK-BALIK publik→ERP→publik), `iteration_88.json` (**0 bug** — `/booking` end-to-end BK-0068 + promo GATHERING500 menurunkan total Rp 5.850.000→Rp 5.350.000, STORY H dua arah, panel Hold Hangus cocok dengan API). Booking uji BK-0068 sudah hilang otomatis saat gate me-reseed data demo. WA/Meta/Google/GA4 tetap **MOCK**. **BERIKUTNYA (antrean `HANDOFF.md` §4):** kredensial nyata (P1), load test konkurensi (P1), halaman promo publik `/promo` (P2), notifikasi proaktif hold hangus (P2). ⚠️ **Anti-bug-palsu untuk sesi berikut:** (a) `backgroundColor` panel CTA memang `rgba(0,0,0,0)` karena latarnya `background-image` — periksa `backgroundImage`, bukan `backgroundColor`; (b) di skrip Playwright async **WAJIB** `await page.set_viewport_size(...)` — tanpa `await` viewport tidak berubah dan pengukuran mobile jadi palsu; (c) `page.inner_text()` juga harus di-`await`; (d) semua Select adalah Radix (klik trigger → klik opsi ber-testid).
- **RESTORE ke pod baru + FASE BOOKING-V2 SELESAI & TERVERIFIKASI (2026-08-12).** Repo `wajauhmawa/travel` di-restore ke `/app` (`bash scripts/bootstrap.sh --seed`; `.env` environment DIPERTAHANKAN, hanya ditambah `SETTINGS_ENCRYPTION_KEY_B64` + `PUBLIC_SITE_URL` + `MEDIA_BACKEND=local`). **Verifikasi dilakukan LEBIH DULU sebelum menyentuh kode:** POC `scripts/test_core_booking_v1.py` **74/74** + `bash scripts/gate.sh` **HIJAU (0 FAIL, 0 SKIP)**. **(1) STORY H** (penutup sisa verifikasi UI BOOKING-V1 -> §0.2 kini TUNTAS): toggle "Tayang di web" (`publish_to_web`) dibuktikan dua arah di UI - dimatikan -> unit hilang dari katalog `/fleet` DAN dari `POST /public/booking/search`; dinyalakan -> kembali. **(2) §0.1 NAVBAR PUBLIK DIRAPIKAN (permintaan user, lampu hijau diberikan):** 9 menu + 5 utilitas (14 target klik, 4 label pecah 2 baris di 1920px) -> **5 menu** (Beranda-Armada-Destinasi-Kalkulator-Blog) + **1 aksi utama `nav-booking-cta` "Pesan Online"** + WhatsApp sekunder; utilitas (`public-nav-booking-status`, `nav-phone-cta`, `public-nav-login`) pindah ke **bar pengumuman**; Tentang/Kontak cukup di footer (`public-nav-about`/`public-nav-contact`); drawer ponsel dapat grup kedua `public-mobile-help-group`; bar bawah ponsel dapat `sticky-cta-booking`; header terukur **90,75 px** (1 baris); `/lp/:slug` tetap ringkas (INV-LP). Seluruh `data-testid` lama DIPINDAH, tidak dihapus. SSOT: `docs/05_NAVIGATION_MAP.md` **§1a**. **(3) Section beranda `home-booking-steps`** "Pesan online dalam 3 langkah" (angka DP & lama hold dari `/public/booking/config`, bukan hardcode). **(4) Chip `resume-booking-chip` "Lanjutkan pesanan BK-00xx"** dari `localStorage` - jaring pengaman agar tamu yang baru transfer tetap menemukan halaman unggah bukti DP; token mati (404) dibuang sendiri. **(5) 3 FITUR BACKLOG pilihan user:** daftar promo bisa diklik di wizard (`POST /api/public/booking/promos` - subtotal DIHITUNG ULANG server, promo tak layak tampil beserta ALASANNYA) - laporan **"Hold Hangus"** (`services/hold_report.py` + `GET /api/reports/hold-expired` + `/export` CSV + panel di `/app/reports`; angka kunci `with_proof` = hangus padahal bukti sudah diunggah = keterlambatan ops) - katalog **11 bandara** untuk tambah-cepat rute + tombol **Arah balik** (tarif per tipe unit ikut tersalin; TARIF tetap diisi pemilik, sistem sengaja tidak menebak harga). **Bug ditemukan & ditutup:** **BUG-0120** (kode rute otomatis simetris `DPS-DPS` -> arah balik mustahil; kelas sama mengancam Bandung-BDO/Solo-SOC/Semarang-SRG). **Guardrail diperkuat:** INV-BOOK-02 statik kini mengawal **SEMUA** skema yang diimpor `routers/booking_public.py` (daftar diturunkan dari `import`, bukan manual) - self-test: `BookingPromoListRequest.subtotal` disuntikkan -> MERAH, revert -> HIJAU (71 cek). **Verifikasi akhir:** `gate.sh` **HIJAU (0 FAIL, 0 SKIP)** - POC **74/74** (ulang) - `ux_audit --strict` **0 ERROR 0 WARN** - testing agent `iteration_83.json` (navbar+STORY H) & `iteration_84.json` (3 fitur: backend 14/14, frontend 100%, **0 bug**). Seed menambah BK-0009/BK-0010 (hold hangus) agar laporan tidak kosong pada data demo. WA/Meta/Google/GA4 = **MOCK**. **BERIKUTNYA:** antrean `HANDOFF.md` §0 - kredensial nyata (P1), load test konkurensi (P1), halaman promo publik `/promo` (P2), notifikasi proaktif hold hangus (P2), isi rute bandara nyata + tarif (kerja user). WASPADA anti-bug-palsu: input tanggal `/booking` TIDAK punya atribut `name` (pakai `data-testid="booking-start"/"booking-end"` + `page.fill`); `hold-range` adalah Radix Select (klik trigger -> klik `hold-range-opt-<n>`); chip "Lanjutkan pesanan" harus diuji dengan pesanan BARU (kode lama hilang saat reseed -> itu self-healing, bukan bug).
- **RESTORE ke pod baru + FASE BOOKING-V1 (Pemesanan Online publik + Verifikasi DP ops) SELESAI & TERVERIFIKASI (2026-08-12).** Repo `djkakajshx/travel` di-restore ke `/app` (`bash scripts/bootstrap.sh --seed`; `.env` environment dipertahankan, ditambah `SETTINGS_ENCRYPTION_KEY_B64` + `MEDIA_BACKEND=local`). **Pertanyaan user:** sesi lalu berhenti saat `/booking` menampilkan "Tidak ada unit bebas di tanggal itu" -> **diverifikasi: BUKAN bug** (seed sengaja penuh 10-17 Agu 2026: BK-0003/BK-0004 memakai V-01/V-02, V-03 perawatan 11-14 Agu). Dengan jendela bebas -> 4 unit tampil, BK-0016 dibuat (hold + countdown DP + rekening + unggah bukti). Armada demo TETAP 3 unit (keputusan user). **Bug NYATA ditemukan & ditutup:** BUG-0114 (SEMUA field teks tanpa `max_length` -> nama 60.000 karakter TERSIMPAN dari guardrail adversarial dan merusak tata letak tabel Booking ERP; 170 field dibatasi + `DataTable` truncate + guardrail **INV-STR-01**), BUG-0115 (unit uji "Smoke Vehicle" TAYANG & bisa dipesan di `/booking` -> `publish_to_web` wajib `True` eksplisit + smoke bersih-bersih sendiri), BUG-0116 (`mutation_smoke` jadi SKIP palsu 409 -> `presweep()`, kini SKIP 0), BUG-0117 (ringkasan wizard `- -> -` + daftar tipe unit hilang tanpa penjelasan; penyebab `ux_audit --strict` MERAH), BUG-0119 (**8 permukaan BACA bocor ke `marketing_admin`**: booking/availability/detail, armada, sopir, perawatan, GPS, penawaran, ruang kerja sopir -> semua dipagari `require_section`, `quotations` dipindah dari section `crm`, dan **INV-RBAC-06 diperluas jadi MATRIKS PENUH 27 section x 4 peran = 155 cek**). **Regresi yang saya sendiri buat & sudah diperbaiki (jujur):** BUG-0118 - `tripLabel` membaca `search.origin` di badan render padahal `search` null sampai config termuat -> `/booking` blank total; ditemukan testing agent; jebakan lanjutan: HMR menyajikan bundle lama sehingga fix seolah gagal -> `supervisorctl restart frontend`. **Fase 3 & 4 plan.md ditutup:** guardrail **INV-PRICE-01** + **INV-BOOK-02** + **INV-STR-01** + `selftest_booking_guards.py` (5 mutasi MERAH<->HIJAU) ter-wire di `gate.sh`; `INV-META-01` diperluas mendukung self-test 1:N (`COVERS`); docs `03_DATA_MODEL` §6 + `04_API_CONTRACT` §5 + `05_NAVIGATION_MAP` diperbarui; `DELIVERY_MANIFEST` fase **BOOKING-V1** P0 **33/33**; `memory/test_credentials.md` diisi. **Verifikasi:** POC `scripts/test_core_booking_v1.py` **74/74** · `bash scripts/gate.sh` **HIJAU 37/37 (0 FAIL, 0 SKIP)** · `ux_audit --strict` **0 ERROR 0 WARN** · `scripts/audit_rbac_sections.py` "sesuai matriks" · `testing_agent_v3` `iteration_81` **0 isu**. WA/Meta/Google/GA4 = **MOCK**. **BERIKUTNYA (permintaan user, BELUM dikerjakan): rapikan navbar situs publik** - analisis + rencana eksekusi di `HANDOFF.md` **§0.1** & `memory/ENHANCEMENT_BACKLOG.md` **§D1**; lalu sisa verifikasi UI 8 user story (`HANDOFF.md` §0.2). ⚠️ Anti-bug-palsu: input tanggal `/booking` TIDAK punya atribut `name` - pakai `data-testid="booking-start"/"booking-end"`.
- **RESTORE ke environment Emergent baru + CMS CW0 HARDENING (CMS-D3 Paginasi) + VERIFIKASI RECHARTS SELESAI (2026-07-17).** Repo `sudahenggaktaulagimaunamainapa/travel` disalin PENUH ke `/app` (rsync; `.env` env dipertahankan: `MONGO_URL`/`DB_NAME`/`REACT_APP_BACKEND_URL`; `.git`/`node_modules` dikecualikan). Deps: backend `pip install -r requirements.txt` (reportlab 5.0.0/openpyxl 3.1.5 dll.), frontend `yarn install --frozen-lockfile` sukses. Backend + frontend **RUNNING**. `bash scripts/gate.sh` → **HIJAU 22/22 gate (0 SKIP, 0 FAIL)**. Akun demo: owner@/ops@/driver@demo.local · `demo12345`. **Pekerjaan sesi ini (user pilih C+D):** **(C) Recharts warning `width/height -1`** — verifikasi console browser (Dashboard + Reports): TIDAK ADA warning (sudah tertangani `initialDimension` prop dari sesi sebelumnya). **(D) CMS-G10 CW0 Hardening (CMS-01) SELESAI:** verifikasi ulang **CMS-D1** (non-numeric `days:"abc"` → 400 pesan jelas, bukan 500) dan **CMS-D2** (duplikat slug → 409) — keduanya sudah bekerja dari G1/R6-5 fix sebelumnya. **CMS-D3 (Paginasi + search) IMPLEMENTED baru sesi ini:** (Backend `routers/content.py`) `GET /content/{resource}` sekarang menerima `limit` (default 100, 1..500) + `offset` (>=0) + response headers `X-Total-Count`/`X-Limit`/`X-Offset` via MongoDB `.skip().limit()` + `count_documents()`; (Backend `server.py`) CORS `expose_headers=["X-Total-Count","X-Limit","X-Offset"]`; (Frontend `ContentManager.jsx`) info "Menampilkan X dari Y" + tombol "Muat lebih banyak (Z tersisa)" (append pattern) + reset saat tab/search berubah + `data-testid`: `content-count`, `content-load-more`. **Verifikasi:** curl 5 skenario (limit=2 offset=2, q=Bali, limit=600→422); Playwright: 6→66 artikel seed → info "Menampilkan 50 dari 66" → klik load-more → "Menampilkan 66 dari 66"; `gate.sh` **HIJAU 22/22 pasca-perubahan (no regression)**. Fix lint sekaligus: 6 bare `except:` → `except Exception:` di test files (backend_test_driver_delete_fix, backend_test_e17_e18_e19, backend_test_negative_amounts, backend_test_whatsapp_regression). WA = **MOCK**. **BERIKUTNYA: menunggu keputusan user** — CMS CW1 (SEO Toolkit CMS-02 ⭐ / Upload CMS-03 / Promo fungsional CMS-04 / Draft workflow CMS-05) atau prioritas lain.

## STATUS SEBELUMNYA
- **EKOSISTEM GUARDRAIL "ULTIMATE" (pre-dev + post-dev + AI-quality + bug-hunt SOP) DIBANGUN (2026-07-05).** Diadaptasi + diperluas dari referensi `github.com/wiisawaslana/kn` (yang sendiri turunan repo travel ini). **4 LAYER:** **(A) PRE-DEV** — `scripts/preflight.py` (Definition of Ready + status artefak) + `docs/16_DEV_LIFECYCLE_SOP.md`. **(B) AI WORK-QUALITY** — guardrail baru **INV-QUALITY-01** (`verify_effort_quality.py`): blokir kerja-dangkal berat (`NotImplementedError` di router, bare `except: pass`, rahasia/URL backend hardcoded) + advisory ringan (TODO/console.log/print). Menemukan+menutup 1 bug nyata **QUAL-1** (`services/whatsapp.py:317` bare swallow → log). **(C) POST-DEV** — guardrail baru **INV-AUTH-01** (`verify_auth_coverage.py`): 267 endpoint dipindai → 243 wajib-auth SEMUA aman, 24 publik via allowlist ber-justifikasi; + hygiene `scripts/find_dead_services.py` (report-only). **(D) BUG-HUNT SOP** — `docs/17_BUGHUNT_SOP.md` (matriks serangan A–L, template testing_agent, honest-assessment, format BUG_REGISTRY, cara upgrade) + `scripts/run_forensics.sh` (deep hunt) + probe `forensic/fa_5xx_fuzz.py` & `forensic/fa_rbac_matrix.py`. **Semua guardrail baru self-test-proven + di-wire ke gate.sh + didaftar INVARIANTS.md (dipaksa INV-META-01).** **Bukti mekanisme jalan:** `fa_5xx_fuzz` = 117 endpoint × 6 payload = **702 probe → 0 5xx**; `fa_rbac_matrix` = 41 endpoint sensitif → driver diblokir semua, owner tak crash. **`bash scripts/gate.sh` → HIJAU 22 gate (0 SKIP).** Belum menjalankan 1 putaran hunt penuh (per keputusan owner: cukup bangun mekanismenya). WA=MOCK. **BERIKUTNYA: menunggu instruksi user.**

- **PUTARAN 12 — PENGUATAN MITIGASI + VERIFIKASI AKHIR SELESAI (2026-07-05).** Menjawab pertanyaan Owner ("zero bug atau minimum bug?"): **JUJUR = "minimum known-bug", BUKAN zero bug** — semua bug DIKETAHUI (katalog §7) ditutup & dijaga preventif, tapi kelas BARU tak bisa dijamin nol; gap jujur: real WA (MOCK), uji beban konkuren tinggi, OSRM/geocode live. **(A) Penguatan mitigasi:** meta-guardrail baru **INV-META-01** (`scripts/guardrails/verify_guardrail_registry.py`) — tiap `verify_*.py` WAJIB ter-wire di `gate.sh` + deklarasi INV-ID + terdaftar di `memory/INVARIANTS.md` → cegah "guardrail hilang/mati diam-diam antar-sesi" (akar masalah sistemik). Self-test: guardrail tak ter-wire → MERAH → hapus → HIJAU. **(B) Dokumentasi pembelajaran & acuan mitigasi:** `docs/15_POSTMORTEM_AND_MITIGATION.md` (root cause sistemik, taksonomi kelas-bug, arsitektur 3-lapis+meta, matriks kelas→invariant→penjaga, playbook tambah invariant, confidence jujur) + terindeks di `docs/00_INDEX.md`. **(C) `bash scripts/gate.sh` → HIJAU 20 gate (0 SKIP).** **(D) Verifikasi AKHIR `testing_agent_v3` iter-66:** regresi lintas SEMUA modul utama (auth/RBAC 3 peran, customers+ID-RACE konkuren, quotations+PDF EXPORT-1, dispatch, finance, payroll, CRM+R6-4, CMS+R6-5, settings+SET-1, publik) + **FE smoke 100%** → **0 bug kritis, no-5xx, regresi aman** (10 "gagal" = isu harness test: path/skema, bukan bug app). Minor kosmetik tersisa: warning recharts `width/height -1` (LOW). **Confidence ~96%.** WA = MOCK. Akun demo: owner@/ops@/driver@demo.local · `demo12345`. **BERIKUTNYA: menunggu instruksi user** (opsional ke ~97%+: uji beban + real WA + fix kosmetik chart).

- **PUTARAN 12 — PHASE-1 PERBAIKAN BUG (M1 + ID-RACE + SET-1) SELESAI & TERVERIFIKASI (2026-07-05).** Menutup **3 bug terbuka TERAKHIR** dari katalog audit §7 (semua temuan §7 kini DITUTUP). **(1) M1** — gate kontrak FE↔BE hijau-palsu: `scripts/verify_api_contract.py` dulu hanya cari `axios.`/`fetch(` → 0 call terdeteksi (FE pakai `apiClient.<m>()`). FIX: deteksi `apiClient|axios` semua gaya kutip + `fetch`, prefix `/api` utk path relatif, buang query menempel (`/crm/rfm${q}`), pencocokan **berbasis-segmen** (`{p}`/`{param}`=wildcard, arity+literal cocok). Kini **260 call (167 path) tervalidasi**, 0 mismatch nyata. **(2) ID-RACE** — `POST /customers`+`ensure_customer` find-then-insert (TOCTOU) → customer ganda. FIX: index UNIK-PARSIAL `customers.phone_normalized` (`$gt ""`) di `server.py` startup + tangani `DuplicateKeyError` (`ensure_customer`→re-find pemenang; `create`/`update`→409). **(3) SET-1** — tarif `settings.pricing_rules`/`pricing_defaults` tak divalidasi → harga negatif/`dp_percent`>100/koersi-0 senyap. FIX: `_validate_pricing` di `routers/settings.py` → 400 pesan jelas. **Guardrail v2 +2 penjaga baru** (statik+runtime, self-test-proven): **INV-IDENT-01** (`verify_identity_race.py`: drop-index→MERAH→HIJAU), **INV-SET-01** (`verify_settings_bounds.py`: disable→MERAH→HIJAU); M1 self-test sisip-route-ngawur→MERAH→HIJAU. Didaftar di `memory/INVARIANTS.md` + `scripts/gate.sh` + `memory/BUG_REGISTRY.md`. **`bash scripts/gate.sh` → HIJAU 19 gate (0 SKIP)**; verifikasi `testing_agent_v3` **iter-65 = Backend 100% (22/22)**, 0 bug kritis, regresi aman. WA = **MOCK**. Akun demo: owner@/ops@/driver@demo.local · `demo12345`. **BERIKUTNYA: menunggu instruksi user** — sisa (opsional): uji beban konkuren tinggi + real WhatsApp (kredensial Meta) utk ~97%+.

- **RESTORE ke environment Emergent baru + FINALISASI ROUND 11.6 SELESAI (2026-07-05).** Repo `akugendutkayababi/travel` disalin PENUH ke `/app` (rsync; `.env` env dipertahankan: `MONGO_URL`/`DB_NAME`/`REACT_APP_BACKEND_URL`; **`.git` riwayat PENUH disertakan atas permintaan user**; `node_modules` dikecualikan). Deps dipasang: backend `pip install -r requirements.txt` (reportlab 5.0.0/openpyxl 3.1.5 dll.; `emergentintegrations`/`litellm` tetap di-comment), frontend `yarn install` (sukses). Backend+frontend **RUNNING**; DB di-seed via `scripts/gate.sh`. **`bash scripts/gate.sh` → HIJAU (17 gate, 0 SKIP)** — termasuk Guardrail v2: INV-NUM-01, INV-RACE-01, INV-RBAC-01/02/03 (statik) + INV-5XX-01 (runtime: EXPORT-1 PDF markup→200, R6-4 preview malformed→200, R6-5 field angka non-numerik→400, expense/booking negatif→422). **3 fix 5xx (EXPORT-1/R6-4/R6-5) diverifikasi ulang di environment ini via `testing_agent_v3` iter-64 = Backend 100% (16/16), 0 bug kritis, regresi jalur normal aman** (lihat `test_reports/iteration_64.json`). Kode produksi **TIDAK diubah** (git bersih; hanya `yarn.lock` untracked). WA = **MOCK** (per keputusan user). Akun demo: owner@/ops@/driver@demo.local · `demo12345`. **BERIKUTNYA: menunggu instruksi user** — item terbuka minor tersisa: ID-RACE/SET-1/M1, uji beban konkuren tinggi, real WhatsApp send (butuh kredensial Meta WABA).

- **RESTORE ke env baru + LANJUTKAN E20 & E21 SELESAI (2026-07-03).** Repo `ferryirwandiagentsoros/rahaza` disalin PENUH ke `/app` (rsync; `.env` env dipertahankan: `MONGO_URL`/`DB_NAME`/`REACT_APP_BACKEND_URL`; `.git`/`node_modules` dikecualikan). Deps FE+BE dipasang; DB di-seed. **Sesi sebelumnya terhenti karena syntax error pada `routers/bookings.py`** — dari repo yang di-clone sudah bersih (E20 backend sudah final di commit terakhir). Yang diselesaikan sesi ini:

  **E20 — Booking Rombongan (Multi-Unit / Multi-Leg):**
  - Backend `POST /api/bookings/group` + schema `GroupBookingCreate/GroupUnit` sudah ada (dari repo).
  - **Verifikasi self-test (curl):** 2 unit → 2 booking anak dgn `group_id=grp_...`, `group_size=2`, `group_index=1|2`, `grand_total` sesuai; per-unit INV-4 + intra-group anti-overlap (armada sama + waktu tumpang tindih) ditolak.
  - **FE baru:** `components/app/GroupBookingDialog.jsx` — repeatable unit rows (tambah/hapus), per-row availability check ke `/bookings/availability`, per-row auto-price `/pricing/quote`, deteksi bentrok dalam grup, grand-total live, wajib DP toggle. Tombol **"Buat Rombongan"** ditambah di `/app/bookings` (di samping "Buat Booking"). Kolom Kode diperkaya **badge "Grup x/y"** utk booking dgn `group_id`.

  **E21 — Pembatalan dgn Denda + Refund Manual:**
  - Backend `POST /api/bookings/{id}/cancel` kini menerima body opsional `CancelBooking{reason, cancellation_fee, refund_amount}`. Validasi: nilai negatif → 400; refund > `paid_amount` → 400 (echo paid). Simpan snapshot `cancellation_reason/cancellation_fee/refund_amount/cancelled_at/cancelled_by`. Event `booking.cancelled` payload +3 variabel untuk template WA. **Backward-compat**: tanpa body → perilaku lama (200).
  - **FE baru:** `components/app/CancelBookingDialog.jsx` — form denda + refund + alasan + preview total/terbayar + validasi refund > paid client-side. Tombol **"Batal"** di baris booking sekarang membuka dialog (bukan `act()` langsung).
  - **Akuntansi kas jurnal refund/denda di-DEFER** (per keputusan user) → `memory/HANDOFF_E21_REFUND.md` (opsi: refund via `payments` negatif, denda sbg memo/invoice; rekomendasi + trade-off didokumentasikan).

  **Docs & guardrail:**
  - `docs/03_DATA_MODEL.md` §bookings ditambah field E20 (`group_id`/`group_size`/`group_index`) + E21 (`cancellation_reason`/`cancellation_fee`/`refund_amount`/`cancelled_at`/`cancelled_by`).
  - `memory/DELIVERY_MANIFEST.md` ACTIVE_PHASE = **E21**; PHASE E20 + PHASE E21 didaftar dgn P0 items.
  - Event `booking.cancelled` sudah ada di `services/events.py::EVENT_TYPES`.

  **Verifikasi:** `bash scripts/gate.sh` **HIJAU** (10 gate; verify_delivery P0 3/3 = 100%; audit_endpoint_sweep 0×5xx; verify_schema 77 PASS; state-machine + concurrency + cross-entity PASS; validate_compliance PASS 25). Screenshot dialog Buat Rombongan + Batalkan Booking terverifikasi via Playwright.

  Akun demo: owner@/ops@/driver@demo.local · `demo12345`. WA = MOCK. **BERIKUTNYA:** menunggu instruksi user (E20/E21 aksi lanjutan: FE regression testing_agent bila diminta, atau lanjut ledger posting refund/denda per `HANDOFF_E21_REFUND.md`).

## STATUS TERKINI
- **PHASE E16 \u2014 PINJAM ARMADA / SUB-CHARTER SELESAI & TERVERIFIKASI (2026-07-02).** Modul sourcing armada mitra saat unit sendiri penuh. **Koleksi baru**: `partners`(ptn_), `subcharters`(sbc_), `partner_settlements`(pst_); `vehicles` +`ownership`(owned|partner)+`partner_id`. **Backend**: `routers/partners.py` (CRUD mitra + AP summary + settlements), `routers/subcharters.py` (siklus requested\u2192confirmed\u2192settled/cancelled + available-partners), `services/subcharter.py` (AP dinamis + COGS + payload WA mitra). **COGS**: confirm \u2192 `expenses`(category `sewa_mitra`, booking_id[+trip_id]) \u2192 P&L/INV-5. **AP (INV-E16)**: ap_total=\u03a3 cost confirmed+settled; ap_paid=\u03a3 settlements; outstanding=selisih. **WA ke MITRA** (payload.phone=telepon mitra) via event `subcharter.requested`/`subcharter.confirmed` + 2 automation rule default (MOCK). **RBAC** section `partners` (owner/ops_admin; driver 403). **FE**: menu \"Pinjam Armada\" (grup Operasional) \u2192 `/app/partners` (tabs Mitra&Utang + Order Sub-charter, dialog PartnerForm/SubcharterForm/Settlement + PartnerDetailDrawer). **Guardrail diperbarui**: verify_contract/verify_schema(prefix/FK/numeric)/validate_compliance CANONICAL + docs 03_DATA_MODEL/05_NAV + DELIVERY_MANIFEST(ACTIVE_PHASE=E16, 8/8 P0). **Verifikasi**: `gate.sh` VERDICT HIJAU (0 orphan) + `testing_agent_v3` iter49 = Backend 100% (65/65) & Frontend 100% (0 bug). Seed: 2 mitra + 1 armada mitra + SC-0001 confirmed (Rp 2.200.000). **BERIKUTNYA (backlog)**: E17 (CRM/WA gaps: WA booking dibatalkan + POD thank-you + reschedule), E18 (DP gate hold auto-expire), E19 (public self-booking).
- **RESTORE ke env baru (repo `sayaakanlawanjilid2/travel`) + HARDENING RBAC/SSOT SELESAI (2026-07-02).** Repo disalin PENUH ke `/app` (rsync; `.env` env dipertahankan: `MONGO_URL`/`DB_NAME`/`REACT_APP_BACKEND_URL`; `.git` dikecualikan). Deps: backend `pip install -r requirements.txt` (reportlab 5.0.0 dll.), frontend `yarn install`. Backend+frontend RUNNING. `bash scripts/gate.sh` → **HIJAU** (health 13/13, 0×5xx, mutation_smoke PASS). **Batch 1 audit lintas-modul TUNTAS & TERVERIFIKASI (`testing_agent_v3` iter48 = Backend 100%):** (1) **RBAC bookings** — 5 endpoint mutasi (`create/patch/confirm/cancel/complete`) kini `require_role("owner","ops_admin")` via konstanta `MANAGER` (driver 403, owner/ops 200, GET tetap read-only). (2) **Ownership driver.py** — `checkin`/`checkout` kini resolve driver + tolak trip milik driver lain (403); manajer boleh atas nama. (3) **SSOT WA payload** — `driver.py::_emit_trip` kini pakai `services.wa_payload.build_wa_payload` (event trip.started/arrived/completed bawa `company/destination/driver_phone/pickup` lengkap; tak lagi drift). (4) **locations.py** — driver hanya boleh POST /locations utk trip miliknya (cegah manipulasi status via geofence). (5) **notifications.py** — `/{id}/read` & `/{id}/dismiss` discope ke `visibility_query` (driver 404 utk notif target 'manager'). Issue 4 (duplikasi `_upsert_notification`) SUDAH beres sejak awal (automation delegasi ke `notifications._upsert`). Akun demo: owner@/ops@/driver@demo.local · `demo12345`. WA = MOCK. **BERIKUTNYA: menunggu instruksi user — kandidat fitur di `ENHANCEMENT_BACKLOG.md` (E16 Pinjam Armada, E17 CRM/WA gaps + reschedule, E18 DP gate, E19 public self-booking).**
- **RESTORE ke environment Emergent baru + VERIFIKASI E14 SELESAI (2026-07-01).** Repo `tidaktaukoktanyasayajokowi/travel` disalin PENUH ke `/app` (rsync; `.env` environment dipertahankan: `MONGO_URL`/`DB_NAME`/`REACT_APP_BACKEND_URL`; `.git`/`node_modules` dikecualikan). Deps dipasang: backend `pip install -r requirements.txt` (reportlab 5.0.0/openpyxl 3.1.5 dll.), frontend `yarn install` (sukses). Backend & frontend **RUNNING**. DB di-seed via `bash scripts/gate.sh` → **HIJAU 10/10** (health_check + audit_endpoint_sweep 0×5xx + mutation_smoke PASS; 32 invarian integrity PASS). **Verifikasi E14 yang tertunda kini TUNTAS:** `testing_agent_v3` **iter 46 = Backend 100% (50/50) + Frontend 100%** — (A1) guard RBAC terpusat: 11/11 URL manager-only me-redirect driver ke `/app/dashboard`; (B1) reminder SIM driver (`sim_reminder` target manager) muncul di `/api/notifications` + NotificationBell; regresi endpoint kritis & halaman 100% (0 bug, 0 console error). Akun demo: owner@/ops@/driver@demo.local · `demo12345`. WA = MOCK. **BERIKUTNYA: menunggu instruksi user untuk fitur/enhancement baru.**
- **E14 (Hardening dari audit menyeluruh) SELESAI 2026-07-01.** Hasil audit modul: (A1) **Guard RBAC terpusat** — `ProtectedRoute` hanya cek login, dan hanya Finance/Reports yang punya guard per-halaman; halaman manager-only lain (customers/crm/quotations/inbox/automation/dispatch/cms/users/settings/auditlog) saat diakses via URL langsung oleh driver menampilkan kartu error "Gagal memuat/Akses ditolak" (bukan redirect). FIX: guard terpusat di `components/app/AppShell.jsx` (peta `pageId` dari path → `canAccess(role,pageId)`; tak berhak → `Navigate /app/dashboard`). (B1) **Reminder SIM driver** — sebelumnya hanya dokumen armada yang di-reminder; kini `services/notifications.py::scan` memindai `drivers.sim_expiry` (≤30 hari/overdue) → notifikasi `sim_reminder` (target manager) + event `driver.sim_expiring` (registered di `services/events.py::EVENT_TYPES`). `NotificationBell.jsx` menambah ikon/tint `sim_reminder`+`invoice_overdue`+`payroll_reminder`. (C) Dead code dihapus: `features/app/Leads.jsx` + import `PlaceholderPage` (dan file `PlaceholderPage.jsx`); `Customers.jsx` LTV pakai `tabular-nums`. Seed `Driver Dua` sim_expiry = +20 hari (agar reminder aktif/teruji). **Verifikasi:** POC `test_core_e14.py` **10/10 PASS**; `bash scripts/seed_reset.sh` LULUS; backend & FE compile bersih. Manifest ACTIVE_PHASE=E14 (DONE). **BERIKUTNYA: jalankan `bash scripts/gate.sh` + `testing_agent_v3` (A1 redirect via browser + B1 SIM reminder + regresi).**
- **E13 (Laporan Driver — Leaderboard + Revenue-per-Trip) SELESAI (2026-07-01).** Opsi terakhir gap-analysis. Dibangun: backend `services/driver_report.py` + endpoint `GET /api/reports/drivers[/export?format=pdf|excel]`, di-merge ke `/reports/summary` (key `drivers_report`). FE section "Laporan Driver (Leaderboard)" di `/app/reports` (4 KPI + tabel leaderboard + Export Excel/PDF). RBAC section `reports` (driver 403). POC `test_core_e13.py` **35/35 PASS**; `testing_agent_v3` iter44 = BE 100% (41/41) + FE 100% + RBAC 100% + Regression 100%.
- **E12 (Payroll Enhancements) SELESAI & TERVERIFIKASI (2026-07-01).** Lanjutan gap-analysis: user pilih 3 enhancement payroll. Dibangun: **(1) Rekap Payroll di modul Laporan** — `services/payroll_report.py` (build+PDF+Excel) + `GET /api/reports/payroll` & `/api/reports/payroll/export?format=pdf|excel`, di-merge ke `GET /api/reports/summary` (key `payroll`); FE section "Rekap Payroll Driver" di `/app/reports` (KPI Total Net/Biaya SDM/Rasio SDM-Revenue/Payout D-S-B + tabel per-driver +YTD + tombol Export Excel/PDF); kategori `gaji_driver` kini tampil di "Pengeluaran per Kategori". **(2) Generate payout MASSAL** — `POST /api/payroll/payouts/generate-bulk` (semua driver, skip existing/idempotent) + FE `PayoutBulkDialog` (tombol "Generate Massal" di tab Payroll). **(3) Otomasi/pengingat payroll** — event `payroll.period_due` (akhir periode) & `payroll.payout_pending` (payout draft/approved menunggu) di `events.py` + reminder di `notifications.scan` (target manager). **Fix RBAC:** halaman Laporan (`Reports.jsx`) di-guard `canAccess(role,'reports')` → driver di-redirect ke `/app/dashboard` (konsisten dgn Finance). Tanpa koleksi baru, tanpa API eksternal. **Verifikasi:** POC `test_core_e12.py` **21/21 PASS**; `bash scripts/gate.sh` **HIJAU** (P0 6/6 = 100%, 0 orphan); `testing_agent_v3` iter43 = **Backend 100% (50/50) + Frontend** (semua fitur jalan; 1 RBAC-UX minor → FIXED & di-verifikasi). Manifest ACTIVE_PHASE=E12 (DONE). Screenshot preview (Rekap Payroll, Generate Massal, driver-redirect) diverifikasi.

## STATUS SEBELUMNYA
- **PHASE C (E11 — Driver Payroll / HR Lite) SELESAI & TERVERIFIKASI (2026-07-01).** Gap analysis 3-fase (B→A→C) kini **TUNTAS**. Dibangun: (a) **kompensasi per driver** embedded `drivers.comp` (gaji pokok bulanan + komisi/trip + komisi %revenue[base trip|booking CONFIGURABLE] + uang jalan/km, toggle per komponen) via `GET/PATCH /api/drivers/{id}/compensation`; (b) koleksi baru **`driver_payouts`** (prefix **`dpo_`** — `pay_` sudah milik payments) + `services/payroll.py` generator akrual dari trip SELESAI + `routers/payroll.py` (`/api/payroll/payouts` list/generate, `/{id}` get/patch/delete, `/{id}/approve`, `/{id}/pay`, `/{id}/slip?format=pdf|excel`, `/api/payroll/summary`); (c) alur **draft→approved→paid**, bonus/potongan **baris manual bebas**; (d) **integrasi Finance (keduanya)**: approve→expense kategori `gaji_driver` (akrual→P&L `pl_full`), pay→`expense.paid=true` (kas), tanpa dobel-hitung; (e) **slip PDF+Excel** (`services/payroll_export.py`); (f) FE: tab **Payroll** di `/app/finance` (`PayrollManager`+`PayoutFormDialog`+`PayoutDetailDialog`) + tab **Kompensasi** di Driver Detail Drawer (`DriverCompDialog`). **Fix RBAC:** halaman Finance kini di-guard `canAccess(role,'finance')` → driver di-redirect ke `/app/dashboard` (ProtectedRoute hanya cek auth, jadi guard per-halaman ditambah di `Finance.jsx`). Guardrails: `driver_payouts` didaftarkan di verify_contract/verify_schema(dpo_ + FK driver/approver/expense)/validate_compliance + INV-24 (payroll akrual/kas sinkron) + seed (comp 2 driver, 2 payout draft+approved, 1 trip SELESAI 250km) + docs/03_DATA_MODEL + DELIVERY_MANIFEST(ACTIVE_PHASE=E11, DONE). **Verifikasi:** POC `test_core_e11.py` **29/29 PASS**; `bash scripts/gate.sh` **HIJAU 10/10** (P0 10/10, INV-24 PASS); `testing_agent_v3` iter41 = BE 100% (68/68) + FE 95% (1 RBAC), iter42 (after fix) = **BE 100% + FE 100%, RBAC RESOLVED**. Screenshot preview (Payroll tab, slip dialog, Kompensasi tab, driver-redirect) diverifikasi. Tanpa API eksternal. Akun demo: owner@/ops@/driver@demo.local · `demo12345`. **BERIKUTNYA: semua 3 fase gap-analysis selesai — menunggu instruksi user untuk enhancement/fase baru.**

## STATUS SEBELUMNYA
- **Restore ke environment Emergent baru + PHASE A (E10) SELESAI & TERVERIFIKASI (2026-07-01).** Konteks: repo `varioksdaslamana/travel` disalin PENUH ke `/app` (rsync, `.env` environment dipertahankan: `MONGO_URL`/`DB_NAME`/`REACT_APP_BACKEND_URL`; `node_modules` FE dipertahankan). Deps dipasang: backend `pip install -r requirements.txt` (→ reportlab 5.0.0/openpyxl 3.1.5), frontend `yarn install` (sukses). DB di-seed via `bash scripts/seed_reset.sh`. Sesi sebelumnya terhenti di tengah wiring frontend Phase A (E10). **Yang diselesaikan sesi ini (frontend Phase A):** (1) `MaintenanceFormDialog.jsx` — dropdown "Jenis" kini menggabung builtin + `service_types` custom aktif (fetch `/service-types?active=true`); (2) `ServiceTypesManager.jsx` + `ServiceTypeFormDialog.jsx` — tab **"Jenis Service"** baru di `/app/maintenance` (CRUD master jenis service + interval km/hari + toggle aktif, RBAC owner/ops_admin, driver read-only); (3) `VehicleDetailDrawer.jsx` — tab **"Riwayat Service"** baru (via `GET /api/vehicles/{id}/maintenance` → 4 stat card count/selesai/total biaya/servis berikutnya + note servis terakhir + tabel maintenance_records armada), tab-switching Riwayat Trip↔Service. **Verifikasi:** POC `test_core_e10.py` **12/12 PASS**; `bash scripts/gate.sh` **HIJAU 10/10** (P0 6/6 100%); `testing_agent_v3` **iter 40 = Backend 100% (44/44) + Frontend 100% (0 bug)** — Service Types CRUD+validasi+RBAC, maintenance form pakai jenis custom, Vehicle drawer Riwayat Service, semua PASS. Screenshot preview diverifikasi. Tanpa API eksternal. Akun demo: owner@/ops@/driver@demo.local · `demo12345`. **BERIKUTNYA (plan.md): PHASE C (E11 — Driver Payroll/HR Lite: kompensasi configurable + `driver_payouts` + integrasi Finance) — menunggu instruksi user.**
- **Fleet Management gap analysis → 3 fase (B→A→C, tanpa API eksternal). PHASE B (E9 — Fleet↔Driver Trip/KM & Odometer) SELESAI & TERVERIFIKASI (2026-06-30).** Latar: user minta analisis gap Fleet (integrasi service, trip/km, payroll driver). Keputusan user: kompensasi driver fully-configurable per driver + gaji pokok + komponen; KM = keduanya (odometer→fallback OSRM); payout periode configurable + approval→Finance; service types cukup master jenis+interval. **Phase B implementasi:** `services/trips.py` (`compute_distance`: odometer end-start → fallback `est_distance_km` OSRM → None; `driver_stats`/`vehicle_stats`); `CheckinRequest.odometer_start/end`; checkout hitung `distance_km`+`distance_basis` & auto-update `vehicles.odometer`; dispatch assign hitung `est_distance_km` (OSRM best-effort). Endpoint baru `GET /api/drivers/{id}/performance` & `GET /api/vehicles/{id}/trips`. FE: `OdometerDialog` (Mulai/Selesai di Driver Workspace + preview jarak), `DriverDetailDrawer` & `VehicleDetailDrawer` (klik baris di /app/drivers & /app/vehicles). Trips fields E9 didokumentasikan; DELIVERY_MANIFEST ACTIVE_PHASE=E9. **Verifikasi:** POC `test_core_e9.py` **14/14 PASS** (E8 tetap 26/26); `bash scripts/gate.sh` **HIJAU 10/10**; `testing_agent_v3` iter 39 = **Backend 100% (40/40) + Frontend 95%** (OdometerDialog UI diverifikasi manual Playwright: Mulai/Selesai + "Jarak tempuh: 250 km"; vehicle odometer 84.000→84.260). **BERIKUTNYA (sesuai plan.md): PHASE A (Service configurable: koleksi `service_types` + riwayat service per-armada di Vehicle Detail) lalu PHASE C (Driver Payroll/HR Lite: kompensasi configurable + `driver_payouts` + integrasi Finance).**
- **ERP E8 — Driver Workspace + Fleet (Servis Preventif Terjadwal + Master Vendor/Bengkel) SELESAI & TERVERIFIKASI (2026-06-30).** Driver Workspace `/app/driver-workspace`; Servis Preventif (interval km+waktu → overdue/due_soon/ok) + jadwalkan; koleksi `workshops` + CRUD + dropdown di form perawatan. POC `test_core_e8.py` 26/26; gate HIJAU 10/10; testing iter 38 BE 100% (50/50) + FE 100%.
- **ERP Transformation E-ADS (Attribution & Marketing) + E6-PREP (WhatsApp Meta Cloud) SELESAI & TERVERIFIKASI (2026-06-30).** Konteks: repo `vriokarbuudahenggakadaide/travel` disalin penuh ke environment Emergent baru (`.env` dipertahankan: `MONGO_URL`/`DB_NAME`/`REACT_APP_BACKEND_URL`); deps dipasang (backend `pip install -r requirements.txt` → reportlab 5.0.0/openpyxl 3.1.5; frontend `yarn install`); DB di-seed via `bash scripts/gate.sh`. Sesi sebelumnya terhenti di tengah end-to-end testing E-ADS/E6 dengan 1 isu frontend dilaporkan ("`/app/leads` redirect ke homepage publik"). **ROOT CAUSE = BUKAN bug:** route `/app/leads` tidak ada — lead ada di **`/app/crm`** (tab Pipeline); URL tak dikenal jatuh ke catch-all `*`→`/`. Sesi sebelumnya memakai URL salah. **Verifikasi sesi ini:** POC `test_core_eads_e6.py` **35/35 PASS**; `bash scripts/gate.sh` **HIJAU 10/10** (0×5xx); `testing_agent_v3` **iter 37 = Frontend 100% (5/5 fitur, 0 bug, 0 console error)** dengan URL benar. WA = MOCK (Meta Cloud kirim NYATA hanya bila kredensial WABA diisi). Akun demo: owner@/ops@/driver@demo.local · `demo12345`.
- **ERP Transformation E5 — Finance Automation SELESAI & TERVERIFIKASI (2026-06-30).** Konteks: repo `variolagikenapasih/travel` disalin penuh ke environment Emergent baru (`.env` dipertahankan: `MONGO_URL`/`DB_NAME`/`REACT_APP_BACKEND_URL`); deps dipasang (backend `pip install -r requirements.txt` → reportlab/openpyxl; frontend `yarn install` → leaflet/recharts/@photo-sphere-viewer); DB di-seed. Pengembangan sebelumnya terhenti tepat setelah membuat 2 komponen finance — sesi ini menyelesaikan **WIRING frontend** + verifikasi. **Backend (sudah ada):** `services/finance_automation.py` (pl_full incl. perawatan total+per-unit [G4]; reconciliation invoice↔pembayaran + `sync_invoice_statuses` paid/partial/sent idempotent [G5]; cashflow bulanan + proyeksi moving-average; ar_overdue + send_ar_reminder/send_bulk_ar_reminders via event `invoice.overdue`→Automation→WA mock; assemble_finance_report) + `services/finance_export.py` (PDF reportlab + Excel openpyxl) + `routers/finance.py` extend (`/finance/pl-full,/reconciliation,/reconciliation/sync,/cashflow,/ar/overdue,/ar/{id}/remind,/ar/remind-all,/export`); `routers/invoices.py` VALID_STATUS += `partial`. **Frontend (sesi ini):** `features/app/Finance.jsx` kini 6 tab (Laba-Rugi · Pengeluaran · Invoice · Piutang · **Rekonsiliasi** · **Arus Kas**) — tab Laba-Rugi di-upgrade ke `pl-full` (Biaya Perawatan + tabel per-unit), tombol **Export PDF/Excel** per periode, tombol **Ingatkan Semua (WA)** + **Ingatkan** per-baris di Piutang + badge usia, status invoice **Sebagian** (partial); komponen baru `FinanceReconciliation.jsx` (tabel + chips + Sinkronkan Status) & `FinanceCashflow.jsx` (KPI + chart Recharts + tabel). **POC `test_core_e5.py` 19/19 PASS.** `gate.sh` **HIJAU 10/10** (0 orphan); `testing_agent_v3` iter 36 = Backend **100% (70/70)** + Frontend **100%** (0 bug), RBAC OK (driver 403). WA = **MOCK**. ACTIVE_PHASE=E5. Akun demo: owner@/ops@/driver@demo.local · `demo12345`. **Berikutnya: E6 / fase lain — menunggu instruksi user.**
- **ERP Transformation E4 — BI & Management Cockpit SELESAI & TERVERIFIKASI (2026-06-30).** Ditambahkan sebagai **tab di Beranda/Dashboard** (`/app/dashboard` → tab "BI Cockpit", owner/ops_admin). Keputusan user: 1b (tab di Beranda), 2b (ad-spend manual per channel → CPL/CAC/ROAS sekarang), 3a (semua panel), 4a (export PDF+Excel), 5b (range 30/90/365 + kustom + pembanding periode lalu). **Backend baru:** `services/analytics.py` (read-only: summary+delta, funnel cohort, channels ROAS/CPL/CAC pakai `settings.marketing_spend`, fleet ROI, driver perf, AR aging, retensi, forecast moving-average) + `services/analytics_export.py` (PDF reportlab + Excel openpyxl) + `routers/analytics.py` (`/api/analytics/{summary,funnel,channels,fleet,drivers,ar-aging,retention,forecast,ad-spend GET+PUT,export}`), section RBAC `analytics` (owner/ops_admin), schema `MarketingSpendUpdate`, wiring `server.py`, seed `settings.marketing_spend`. **Frontend baru:** `Dashboard.jsx` → tab [Ringkasan | BI Cockpit]; `components/app/BiCockpit.jsx` (range picker + KPI eksekutif +delta + export) + `BiCharts.jsx` (funnel/channel/forecast Recharts) + `BiTables.jsx` (fleet/driver/AR aging/retensi) + `AdSpendDialog.jsx`. **POC `test_core_e4.py` 20/20 PASS.** `gate.sh` **HIJAU 10/10**; delivery **15/15 P0** (ACTIVE_PHASE=E4); `testing_agent_v3` iter 35 = Backend fungsional 100% (10 'gagal' = timeout infra saat uji RBAC driver — curl driver=403 ~2.4ms), Frontend 100% (0 bug), RBAC OK. **Catatan:** channel-revenue diatribusikan dari nilai lead *won* (ROAS berbayar penuh menanti E-ADS). marketing_spend = key `settings` (bukan koleksi baru). Akun demo: owner@/ops@/driver@demo.local · `demo12345`. **Berikutnya: E5 (Finance Automation) — menunggu instruksi user.**
- **ERP Transformation E3 — Dispatch & Komunikasi Operasi SELESAI & TERVERIFIKASI (2026-06-30).** Lanjutan E3 yang sempat terhenti tepat saat wiring frontend Dispatch. Konteks: repo `variokarbumberbroom/travel` disalin penuh ke environment Emergent baru (`.env` dipertahankan: `MONGO_URL`/`DB_NAME`/`REACT_APP_BACKEND_URL`). Tindakan sesi: (1) **copy repo** + backend `pip install reportlab openpyxl`; (2) **FIX BUG**: import `Navigation` (lucide) hilang di `config/navigationConfig.js`; (3) **FIX BUILD**: `yarn install` (`leaflet`, `@photo-sphere-viewer/*`) → webpack compile. Backend E3: `routers/dispatch.py`, `services/geocode.py` (Nominatim + cache `geocode_cache`), section 'dispatch', StaticFiles `/api/uploads` (POD), 4 rule WA baru. Frontend E3: `Dispatch.jsx` + `AssignTripDialog` + `PodDialog` + menu `nav-dispatch`. POC `test_core_e3.py` **14/14 PASS**. `gate.sh` HIJAU 10/10; `testing_agent_v3` iter 34 = Backend 100% (26/26) + Frontend 100% (0 bug) + RBAC OK. WA = MOCK.
- **ERP Transformation E2 — CRM Growth Engine (FRONTEND wiring + seed) SELESAI & TERVERIFIKASI (2026-06-30).** Lanjutan E2 yang sempat terhenti tepat setelah testing run (iter 31, PASS 99% — 1 isu minor testid). Tindakan sesi ini: (1) **fix** `CrmReminders.jsx` → root `data-testid="crm-reminders"` di semua state (loading/error/empty/list); (2) **seed E2** ditambah ke `scripts/seed_data.py` (idempotent): 3 segmen ('Semua Pelanggan' all-customer system, 'Lead Hot' lead score_band=hot, 'Pelanggan Bernilai Tinggi' LTV≥1jt), 1 sequence ('Nurturing Lead Baru'), recompute skor lead+RFM (3 lead hot), 1 kampanye contoh TERKIRIM ('Promo Akhir Pekan' → total=3/sent=3/cost=1050, 3 recipients); (3) docs delivery di-update (DELIVERY_MANIFEST E2 P0 22/22, NAVIGATION_MAP tab CRM baru, CODEBASE_MAP komponen+router E2). 7 tab CRM urut: Pipeline · Skor&SLA · RFM · Segmen · Sequence · Campaign · Reminder; tab **Broadcast lama DIHAPUS** dari CRM. **gate.sh HIJAU 10/10**; `testing_agent_v3` **iter 33 = Backend 100% (59/59) + Frontend 100% (0 bug)** — flow Campaign end-to-end (segmen → buat → preview → kirim → recipients) PASS. WA = MOCK (provider mock). ACTIVE_PHASE=E2. **Berikutnya: E3** (menunggu instruksi user; lihat `docs/ERP_TRANSFORMATION_PLAN.md`).
- **Restore ke environment Emergent baru (2026-06-30): SELESAI & TERVERIFIKASI.** Repo `variokarbudamn/travel` disalin penuh ke `/app` (347 file, `.env` environment dipertahankan: `MONGO_URL`/`DB_NAME`/`REACT_APP_BACKEND_URL`). Dependency dipasang: frontend `yarn install` (sukses), backend `pip install -r requirements.txt` (sukses; `emergentintegrations`/`litellm` tetap di-comment). Backend & frontend **RUNNING**. DB di-seed via `scripts/seed_data.py`. **`bash scripts/gate.sh` HIJAU 10/10**. Akun demo: owner@/ops@/driver@demo.local · password `demo12345`.
- **ERP Transformation E1 — Event Bus + Automation Engine + WhatsApp Adapter (mock) + Inbox upgrade (2026-06-30): SELESAI.** Keputusan user (1c/2a/3a/4a): konfigurasi rules di Settings + menu **Otomasi** utk monitoring; seed 8 rule contoh AKTIF; tombol "Simulasi WA Masuk" di Inbox; stub `meta_cloud` (aktif E6). **Event Bus** (`services/events.py`, koleksi `events`): `emit()` idempotent+defensif dipancarkan dari aksi nyata (lead.created/quotation.sent/booking.confirmed/payment.recorded/trip.started+completed/booking.departure_due/invoice.overdue/doc.expiring/wa.inbound). **Automation Engine** (`services/automation.py`, `automation_rules`/`automation_runs`): kondisi→aksi (send_wa/create_notification/create_task/assign_agent/schedule_followup) idempotent (dedupe rule:event) + audit per run. **WhatsApp** (`services/whatsapp.py`): provider-agnostic, MockProvider aktif (cost tracking, session-window 24j, auto-reply, opt-in/out), MetaCloudProvider stub + webhook publik (`/api/wa/webhook` verify+ingest+signature). **Inbox** upgrade: kanal whatsapp kirim via provider, biaya, sesi, template, simulasi inbound. **POC `test_core_e1.py` 23/23 PASS**. `gate.sh` **HIJAU** + delivery 15/15 P0. ACTIVE_PHASE=E1. **Berikutnya: E2** (lihat `docs/ERP_TRANSFORMATION_PLAN.md`).
- **ERP Transformation E0 — Hardening Kepercayaan (2026-06-30): SELESAI & TERVERIFIKASI.** Memulai jalur ERP dari `docs/ERP_TRANSFORMATION_PLAN.md`. G7 bcrypt (+migrasi transparan SHA256→bcrypt saat login) & rate-limit login (8 gagal/IP+email/5mnt→429, sukses membersihkan budget); G3 audit lengkap (auth login/fail/logout, users, maintenance, broadcasts, drivers delete); G8 guard (larang ubah-peran/nonaktifkan diri sendiri + minimal 1 owner aktif); G2 booking `ongoing` NYATA (checkin confirmed→ongoing, checkout→completed/selesai). Gate **HIJAU 10/10**; `testing_agent_v3` iter 29 = 0 bug kritis (48/51 E0-suite). Kredensial uji di `memory/test_credentials.md`.
- **UI/UX publik (2026-06-30):** Polish kaca 3D + pantulan cahaya + shimmer (light-mode, brand azure) di `src/index.css` (token global + kelas `.glass-3d`/`.glass-bevel`/`.shimmer-on-hover`/`.cta-shine`/`.glass-modal`) diterapkan ke kartu/CTA/hero estimator/pop-up semua halaman publik. ux_audit ERROR 0, validate_compliance FAIL 0.
- **Restore (2026-06-30):** Repo disalin ulang ke environment Emergent baru + dependency dipasang (frontend `yarn install`; backend + `openpyxl`/`reportlab`). `.env` environment dipertahankan. Backend & frontend RUNNING.
- **Fase:** Phase 0–9 **SEMUA COMPLETED & TERVERIFIKASI**. Phase 7 (Gap Tier A) **resmi DITUTUP 2026-06-30** (sebelumnya berstatus ACTIVE/belum diverifikasi formal karena ACTIVE_PHASE sempat lompat ke 9).
- **Phase 0** (Auth+RBAC) ✅ · **1** (GPS+ETA+anti double-booking) ✅ · **2** (ERP Core) ✅ · **3** (Website Publik + Lead) ✅ · **4** (CRM) ✅ · **5** (Keuangan & Laporan) ✅ · **6** (Maintenance + GPS lengkap) ✅ · **7** (Gap Tier A: Inbox + Notifications + Settings + AuditLog) ✅ · **8** (Hardening: audit_logs, counters, onboarding) ✅ · **9** (Tahap B: Pricing · Quotation · CMS · Identity) ✅.
- **Gate:** `scripts/gate.sh` → **HIJAU 10/10** (receipt 2026-06-30). Catatan: 1 fix kecil saat restore — `components/public/ScrollStory.jsx` ditambah loading+empty-state agar `ux_audit --strict` lolos.
- **Verifikasi Gap Tier A (testing_agent_v3 iter 25):** Backend 98.6% (73/74; 1 minor note = pilihan desain API, bukan bug) · Frontend 100% (0 bug). Lihat `test_reports/iteration_25.json`.
- **Catatan dependency:** `backend/requirements.txt` punya konflik bawaan `emergentintegrations==0.2.0` vs `litellm 1.80.0` (keduanya TIDAK diimpor kode app) → `pip install -r requirements.txt` penuh gagal resolusi; app tetap jalan penuh. Longgarkan salah satu pin bila perlu install penuh.

## YANG SUDAH SELESAI (Phase 6 — GPS lengkap + Maintenance)
- **Maintenance** `routers/maintenance.py` + `services/maintenance.py`: GET `/maintenance` (filter vehicle_id/status/type), GET `/maintenance/reminders` (KIR/pajak/servis bucket overdue/h7/h14/h30), GET `/maintenance/summary` (KPI), POST/PATCH/DELETE + POST `/maintenance/{id}/complete` (set done + armada→available, catat last_service). Mutasi = owner/ops_admin.
- **INV-21 (window blok booking):** `services/availability.find_maintenance_conflicts` dipakai di `bookings` create + `/bookings/availability`. Unit dgn window scheduled/in_progress yang overlap → POST booking ditolak 400 "Armada dalam masa perawatan"; availability mengembalikan `maintenance[]`.
- **GPS lengkap:** `routers/locations.py` → `/locations/live` kini ada `last_seen/age_seconds/stale/live_status` (moving|idle|offline, STALE>180s); geofence auto-status saat ingest (`services/geofence.py`: tiba pickup→on_trip, tiba tujuan→completed, radius 300m); `/locations/history?vehicle_id|trip_id` (playback lintas-trip).
- **Share-link korporat:** `routers/shares.py` (`shr_`, koleksi `trip_shares`): POST `/shares` {trip_id,label,hours}, GET `/shares` (is_active/expired), POST `/shares/{id}/revoke`, DELETE `/shares/{id}`. Publik (tanpa auth): `GET /api/public/track/{token}` → posisi+ETA+track (403 revoked, 410 expired, 404 unknown). INV-22 jaga token aktif sah.
- **Adapter Maps:** `services/maps.py` (default OSRM/OSM; stub Google Directions siap bila `GOOGLE_MAPS_API_KEY` diisi).
- **Frontend:** `features/app/Maintenance.jsx` (KPI + banner reminder + tabel jadwal/riwayat + `MaintenanceFormDialog`), `GpsTracking.jsx` di-upgrade jadi 3 tab (Live dgn status dot stale/offline · Riwayat playback per-armada · Bagikan = `ShareLinkPanel`), halaman publik `features/public/PublicTrack.jsx` (`/track/:token`, peta read-only auto-refresh 10s). Nav: `maintenance` aktif (comingSoon dilepas).
- **Seed:** +3 `maintenance_records` (1 in_progress blok V-03, 1 scheduled, 1 done history) + 1 `trip_shares` aktif (7 hari). Koleksi kanonik kini **25** (tambah `trip_shares`).

## YANG SUDAH SELESAI (Phase 2 — ERP Core)
- **Backend CRUD penuh:**
  - `routers/vehicles.py` — GET/POST/PATCH/DELETE + GET/{id}; auto-code `V-xx`; plat unik; DELETE diblokir 400 bila armada terkait booking. Mutasi = owner/ops_admin.
  - `routers/drivers.py` — GET/POST/PATCH/DELETE; DELETE diblokir 400 bila driver di booking aktif.
  - `routers/customers.py` — GET/POST/PATCH/DELETE + **Customer 360** (`GET /customers/{id}` → `bookings[]` + `stats{bookings_count,total_spent,active_bookings}`). Section `customers` (owner/ops_admin).
  - `routers/bookings.py` — tambah `PATCH /bookings/{id}` (edit driver/asal/tujuan/catatan) + `GET /bookings/{id}` kini sertakan `payments[]`. Create tetap INV-1/INV-4/INV-10.
  - `routers/payments.py` (BARU) — `GET /payments?booking_id=`, `POST /payments`. Section `finance`.
  - `routers/dashboard.py` — role-aware: tambah `due_soon_count`+`reminders[]` (KIR/pajak/servis ≤30 hari), `recent_bookings[]`, dan (owner/ops) `revenue_month`+`outstanding_ar`. INV-9 tetap utuh.
- **Service baru:** `services/finance.py` — `derive_payment_status` (INV-3) + `recompute_booking_payment` (INV-2: paid=Σpayments, tolak overpay 400). Router tetap thin.
- **Frontend (CRUD UI + states + data-testid):**
  - Pages: `Vehicles.jsx` (+banner reminder due-soon), `Drivers.jsx`, `Customers.jsx` (row→Customer 360), `Bookings.jsx` (aksi Bayar/Edit/Selesai/Batal), `Dashboard.jsx` (KPI + reminders + booking terbaru + chart).
  - Komponen dialog reusable: `VehicleFormDialog`, `DriverFormDialog`, `CustomerFormDialog`, `CustomerDetailDialog` (360), `PaymentDialog` (total/terbayar/sisa + histori), `BookingEditDialog`, dan shared `ConfirmDialog` (hapus).
- **Seed:** kini 3 vehicles (V-03 Elf, KIR due 8 hari → reminder), 3 customers, 2 bookings + 2 payments (DP). `health_check` + `/api/payments` terdaftar kritis.

## YANG SUDAH SELESAI (Phase 3 — Website Publik + Lead)
- **Backend `routers/public.py`** (tanpa auth, prefix `/api/public`): `GET /fleet`,`/fleet/{id}`,`/destinations`,`/destinations/{slug}`,`/articles`,`/articles/{slug}`,`/testimonials`,`/company`; `POST /trip-estimate` (hitung server-side, tarif indikatif); `POST /quotation` → buat `leads` (source=website, stage=new, assigned ops_admin) → tampil di CRM.
- **Frontend publik (dual-surface):** `PublicLayout` (header nav + WA FAB + footer) + 12 halaman: Home (hero+trip-finder+featured fleet/destinasi/testimoni dgn loading+empty state), Fleet, FleetDetail, Destinations, DestinationDetail, TripCalculator, Quotation→ThankYou, Blog, BlogDetail, About, Contact. Routing di `App.js` (publik `/` + ERP `/app/*`).
- **Seed:** destinations(4), articles(3), testimonials(3), company_info (settings), foto armada.
- **Verifikasi:** gate.sh HIJAU 10/10 + testing_agent_v3 iter5 100% + invarian lead-from-website PASS.

## YANG BELUM / BERIKUTNYA (Gap Tier A tersisa — di luar Phase 6)
- **CRM Inbox** (`conversations`/`messages`), **notification_tasks + scheduler**, **Settings admin**, **audit_logs**. Detail: `plan.md` §0.5-C.
- **Saat mulai fase baru:** set `ACTIVE_PHASE` di `DELIVERY_MANIFEST.md`, isi P0, penuhi sebelum klaim selesai, tutup dgn `gate.sh` HIJAU + `testing_agent_v3`.

## CARA MULAI (agent berikutnya)
1. Baca `docs/AGENT_START_HERE.md` → jalankan BOOT SEQUENCE (`bash scripts/load_context.sh`).
2. Ikuti PLAYBOOK sesuai tugas. Tutup dengan `bash scripts/gate.sh` (harus HIJAU 10/10).

## CATATAN PENTING
- **UI ERP** iOS-inspired (sidebar putih glass + grup collapsible, primary azure `#007AFF`). Style di `frontend/src/styles/erp.css`. Public site `/` editorial (Gloock) — TIDAK diubah.
- Auth final: **email + password**, TANPA Google Auth. Akun demo: owner@/ops@/driver@demo.local · password `demo12345`. Login di **`/app/login`** (ERP di `/app/*`, publik di `/`).
- Komunikasi ke user = **Bahasa Indonesia**.
- Peta GPS = **Leaflet + OSM + OSRM (gratis)**.
- `backend_test.py` (diperbarui testing agent) ada di /app — boleh dipakai regresi.
