# Optimasi install: utamakan cache global (offline-first) & beri timeout jaringan besar
# agar `yarn install` di environment baru cepat memakai cache dan tidak gampang timeout.
--install.prefer-offline true
network-timeout 600000
